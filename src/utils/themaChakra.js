import { extendTheme } from '@chakra-ui/react';

export const theme = extendTheme({
	fonts: {
		heading1: `'Fascinate Inline', cursive, sans-serif`,
	},
	config: {
		useSystemColorMode: true,
	},
});
